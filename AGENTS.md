本项目采用 KDD（Knowledge Driven Development）。

开始任何任务前必须阅读：

- docs/knowledge-base/README.md
- docs/knowledge-base/constraints.md
- docs/knowledge-base/glossary.md

---

# 项目工作规则

本项目采用知识库驱动开发模式（Knowledge Driven Development）。

项目知识库是项目事实来源（Source of Truth）。

知识库聚焦代码无法表达的内容：约束、不变量、边界和术语。

所有分析、设计、开发、测试和文档更新工作必须基于知识库进行。

---

# 硬护栏（最高优先级，每次响应前必须执行）

## 1. 声明当前状态（每次响应必须输出）

```text
任务：[一句话描述]
门禁阶段：[知识库阅读 / 影响分析 / 设计方案 / 用户确认 / 开发实现 / 测试验证 / 知识库同步 / 轻量门禁]
```

## 2. 自检（未通过禁止输出代码）

```text
□ 是否已阅读相关知识库文档？
□ 是否已输出影响分析？
□ 是否已输出设计方案？
□ 是否已获得用户确认？
```

**规则**：自检未通过 → 只能输出分析、方案、提问，禁止输出代码。

**无例外**：无论任务大小、无论是否紧急，都必须通过自检才能输出代码。

**默认走完整门禁**：所有任务默认必须走完整门禁流程，不允许跳过任何阶段。

---

# 术语定义

关键术语定义详见 `docs/knowledge-base/glossary.md`，以下为本工作流高频术语速查：

| 概念 | 定义 |
|------|------|
| 能力契约 | 核心层定义的业务能力接口，表达"能做什么" |
| 接口契约 | 对外暴露的 API 接口，表达"如何调用" |

---

# 轻量门禁（仅限纯 UI 调整）

当且仅当同时满足以下全部条件时，可进入轻量门禁：

```text
□ 修改范围仅涉及 UI 样式/布局
□ 不修改业务逻辑/领域模型/接口契约
□ 不新增或删除业务模块文件（纯 UI 组件文件不在此限）
□ 不修改业务数据结构（纯 UI 展示属性变更不在此限，如样式类名、文案、可见性）
□ 不修改状态流转
```

### 判定示例

| 场景 | 判定 | 理由 |
|------|------|------|
| 修改按钮颜色、间距 | 轻量 | 仅涉及 UI 样式 |
| 调整页面布局 | 轻量 | 仅涉及 UI 布局 |
| 修改组件 props 的 `isLoading` 字段 | 轻量 | 纯 UI 展示属性变更 |
| 修改组件 props 新增业务数据字段 | **完整** | 修改了业务数据结构 |
| 新增表单字段且涉及后端校验 | **完整** | 修改了业务逻辑 |
| UI 组件新增状态管理（如 loading/error） | **完整** | 修改了状态流转 |
| 修改列表展示字段（字段来自后端新增） | **完整** | 修改了数据结构 |

轻量门禁只需输出：

```text
任务：[一句话描述]
门禁阶段：[轻量门禁]

【影响分析】

仅 UI 调整，不影响业务逻辑、数据结构、接口契约。

【修改范围】

- 涉及文件：xxx
- 修改内容：xxx

【知识库同步】

□ 是否需要更新经验教训库
□ 是否涉及组件接口变更
```

**任何一项不满足轻量条件，必须走完整门禁。**

**轻量门禁仍需用户确认后才能输出代码。输出后等待用户回复"确认"或"按此方案执行"方可进入开发。**

---

# 必读文档

开始任何任务前，必须优先阅读以下文档：

知识库导航：

```text
docs/knowledge-base/README.md
```

约束（必读）：

```text
docs/knowledge-base/constraints.md
```

术语表：

```text
docs/knowledge-base/glossary.md
```

根据任务内容继续定位并阅读相关知识库文档。

---

# 任务状态机

所有任务必须按以下状态顺序执行：

```text
[知识库阅读]
        ↓
[影响分析]
        ↓
[设计方案]
        ↓
[用户确认]
        ↓
[开发实现]
        ↓
[测试验证]
        ↓
[知识库同步]
        ↓
[任务结束]
```

禁止跳过任何阶段。

未经用户确认：

禁止从设计阶段直接进入开发阶段。

---

# 门禁一：知识库阅读

开始任务后首先进入：

```text
[知识库阅读]
```

必须完成以下操作：

1. 阅读 docs/knowledge-base/README.md（项目概览）
2. 阅读 docs/knowledge-base/constraints.md（必读）
3. 根据任务内容阅读相关条件文档
4. 阅读经验教训库（docs/knowledge-base/lessons/README.md），按业务分类匹配索引
5. 理解现有约束和不变量

---

## 增量阅读

每个新会话的首次任务必须完整阅读必读文档。

同一会话内的后续任务可采用增量阅读：

1. 仅阅读与当前任务相关的文档（根据任务关键词匹配）
2. 如果上次任务修改了某些文档，本次任务涉及相同模块时必须重新阅读

任务相关性判断：

| 任务类型 | 必须阅读 |
|----------|----------|
| 新增业务能力 | `constraints.md`、`boundaries.md`、`flows.md`（如存在） |
| 修改数据结构 | `constraints.md`（不变量部分） |
| 修改 API | `constraints.md`、`boundaries.md`（如存在） |
| 修改架构 | `constraints.md`（架构约束部分） |
| 修复 Bug | `constraints.md`（不变量部分）、`lessons/` |

---

## 读代码

知识库记录代码无法表达的内容，代码能表达的内容必须从代码中获取。

阅读知识库后，必须阅读与任务相关的代码：

1. 阅读涉及修改的模块代码，理解当前实现
2. 阅读依赖和被依赖的模块代码，理解影响范围
3. 阅读相关测试代码，理解现有测试覆盖

禁止仅基于知识库就动手修改代码，必须同时理解代码现状。

### 代码定位方法

按以下顺序定位相关代码，避免遗漏：

```text
1. 修改目标：直接要修改的文件/函数

2. 调用链上游：谁调用了要修改的代码（被依赖方）
   - 搜索函数/方法名的引用
   - 搜索接口的实现类

3. 调用链下游：要修改的代码调用了谁（依赖方）
   - 查看当前代码的依赖引用

4. 测试代码：对应的测试文件
   - 查找同名测试文件（如 foo.ts → foo.test.ts）
   - 搜索引用被修改函数的测试

5. 配置代码：相关的配置/路由/注入
   - 搜索被修改模块的注册/配置位置
```

每一步都必须实际搜索，不得仅凭记忆或猜测跳过。

---

## 必须输出

```text
【已阅读文档】

- constraints.md
- lessons/xxx.md（如存在相关教训）

【现有约束理解】

- [约束1]
- [不变量1]

【经验教训参考】

- 是否存在与当前任务相关的历史教训：是/否
- 相关教训摘要：xxx（若无则填"无"）
```

未输出上述内容，视为门禁未通过。

---

## 门禁未通过时

禁止输出：

* 代码
* 伪代码
* 文件修改方案
* 重构方案

只能继续知识库分析。

---

# 门禁二：影响分析

知识库阅读完成后进入：

```text
[影响分析]
```

必须分析以下内容：

```text
□ 是否违反约束或不变量

□ 是否新增业务能力

□ 是否修改业务规则

□ 是否修改模块职责

□ 是否修改能力契约

□ 是否修改接口契约

□ 是否修改状态流转

□ 修改的代码是否被其他模块依赖（从代码中检查依赖和被依赖关系）

□ 是否需要新增/修改测试
```

---

## 状态机修改专项检查

当影响分析中"修改状态流转"为"是"时，必须额外执行：

```text
□ 列出所有受影响的状态转换路径（不仅是修改的那一条）
□ 检查每条路径是否仍然合法（对照 flows.md 中的转换规则和禁止转换）
□ 识别是否有间接影响：修改 A→B 是否导致 C→A 或 B→D 失效
□ 检查跨模块事件联动（对照 flows.md 跨模块事件联动表）：本次修改的事件是否触发其他模块的联动动作
```

---

## 必须输出

| 检查项 | 是否影响 | 说明 |
| ------ | ---- | -- |
| 违反约束/不变量 | 是/否 | 说明 |
| 新增业务能力 | 是/否 | 说明 |
| 修改业务规则 | 是/否 | 说明 |
| 修改模块职责 | 是/否 | 说明 |
| 修改能力契约 | 是/否 | 说明 |
| 修改接口契约 | 是/否 | 说明 |
| 修改状态流转 | 是/否 | 说明 |
| 影响其他模块 | 是/否 | 列出被影响的模块及原因 |
| 新增/修改测试 | 是/否 | 说明 |

未输出分析结果，视为门禁未通过。

---

## 文档优先原则

如果分析结果显示：

```text
存在设计变更
```

则必须：

```text
先更新设计文档

后修改代码
```

禁止反向执行。

---

## 门禁未通过时

禁止输出：

* 代码
* 文件修改方案

只能继续影响分析。

---

# 门禁三：设计方案

影响分析完成后进入：

```text
[设计方案]
```

必须输出：

---

## 需求理解

| 项目 | 内容 |
|------|------|
| 当前需求 | [一句话描述需求] |
| 解决的问题 | [说明业务价值] |
| 关联约束 | [涉及 constraints.md 中的哪些约束/不变量] |

---

## 修改范围

| 维度 | 涉及内容 |
|------|----------|
| 模块 | [涉及哪些模块] |
| 约束/不变量 | [是否新增或修改约束] |
| 能力边界 | [是否修改能力边界] |
| 状态流转 | [是否修改业务流程] |

---

## 任务范围锁定

| 项目 | 内容 |
|------|------|
| 本次做 | [明确列出本次任务要完成的事项] |
| 本次不做 | [明确列出不在本次范围内的事项] |

开发过程中不得超出"本次做"的范围。发现额外问题时记录到"本次不做"，另建任务处理。

---

## 设计方案

### 职责划分

[每个模块负责什么、不负责什么]

### 核心模型

[核心对象、对象关系、关键约束、业务不变量]

### 协作关系

[模块协作方式、依赖方向、数据流向]

### 实现思路

[如何实现，为什么这样实现]

### 复杂流程

[如有复杂流程，使用 Mermaid 图示]

### 状态机修改（如涉及）

修改状态机时必须输出：

```text
【修改前状态图】
[完整状态图，标注修改点]

【修改后状态图】
[完整状态图，标注变化]

【受影响的转换路径】
| 路径 | 影响 | 是否合法 |
|------|------|----------|
| A→B→C | 新增前置条件 | 合法 |
| C→A | 原路径被阻断 | 需确认 |

【禁止转换检查】
| 禁止转换 | 是否被违反 |
|----------|-----------|
| A→C | 否 |
```

### 扩展点分析

[哪些部分未来可能变化、是否需要立即扩展]

---

## 风险分析

| 风险类型 | 风险描述 | 应对措施 |
|----------|----------|----------|
| 兼容性 | [是否影响现有接口/数据] | [应对] |
| 性能 | [是否有性能影响] | [应对] |
| 扩展性 | [是否限制未来扩展] | [应对] |
| 维护性 | [是否增加维护成本] | [应对] |

---

## 原则冲突裁决

设计原则详见 `docs/knowledge-base/constraints.md`。

当多个原则发生冲突时，按以下优先级裁决：

```text
业务优先 > 职责优先 > 变更成本 > 简单优先 > 扩展优先
```

裁决示例：

- 满足当前需求 + 结构简单 + 扩展一般 **优于** 支持十种扩展 + 结构复杂 + 当前不需要
- 稍微增加设计成本 + 变更仅改 1 个模块 **优于** 开发快 + 变更需改 20 个模块

目标：**最小充分复杂度**

---

## 开发计划

| 步骤 | 内容 | 验证方式 |
|------|------|----------|
| 1 | [实施步骤1] | [如何验证] |
| 2 | [实施步骤2] | [如何验证] |

回滚方案：[如何回滚，具体操作步骤]

---

# 门禁四：用户确认

设计方案完成后：

必须等待用户确认。

---

未获得确认前：

禁止进入开发阶段。

禁止输出：

* 功能代码
* 文件修改内容
* 重构代码

---

# 门禁五：开发实现

获得确认后进入：

```text
[开发实现]
```

开发过程中必须：

1. 遵循 constraints.md 中的架构约束
2. 不得违反业务不变量
3. 不得基于猜测补全业务规则
4. 不得脱离现有架构随意重构
5. 代码能表达的内容不需要写入知识库
6. 任何修改都必须有对应测试，包括受影响的已有功能
7. 不得超出"任务范围锁定"中"本次做"的范围
8. 发现额外问题时记录下来，不得顺手修改

---

## 代码变更自检

代码编写完成后、进入测试验证前，必须逐条检查：

```text
□ 每处变更是否在"本次做"范围内
□ 是否违反 constraints.md 中的禁止事项
□ 是否违反业务不变量
□ 是否引入了不必要的重构或优化
□ 修改的代码是否被其他模块依赖，是否已处理
```

如果自检发现超出范围的变更，必须回退。

---

# 门禁六：测试验证

代码完成后进入：

```text
[测试验证]
```

必须说明：

```text
【测试报告】

| 测试类型 | 测试场景 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|
| 单元测试 | [场景1] | [预期] | [实际] | 是/否 |
| 集成测试 | [场景2] | [预期] | [实际] | 是/否 |
| 回归测试 | [受影响的已有功能] | [预期] | [实际] | 是/否 |

覆盖率：[核心层 xx% / 技术层 xx%]

未覆盖场景（如有）：
- [说明未覆盖的原因]
```

回归测试必须覆盖影响分析中识别出的受影响模块。

---

# 门禁七：知识库同步

开发完成后进入：

```text
[知识库同步]
```

必须检查：

```text
□ 是否需要更新约束/不变量（constraints.md）

□ 是否需要更新术语表（glossary.md）

□ 是否需要更新能力边界（boundaries.md，如存在）

□ 是否需要更新业务流程（flows.md，如存在）

□ 是否需要更新经验教训库（lessons/）

□ 是否需要新增架构决策记录（adr/）
```

知识库更新必须与代码变更在同一批次提交，禁止分开提交导致知识库与代码不同步。

---

## 更新指引

| 文档 | 更新时机 | 更新内容 |
|------|----------|----------|
| `constraints.md` | 架构约束变更、新增/修改不变量、新增禁止事项 | 约束、不变量、禁止事项 |
| `glossary.md` | 新增业务术语/缩写 | 术语定义、缩写表 |
| `boundaries.md` | 新增/修改/删除业务能力 | 能力列表、边界、扩展点 |
| `flows.md` | 业务流程/状态机变更 | 流程、状态流转、禁止转换 |
| `adr/` | 做出重要技术决策时 | 新增 ADR 文档 |
| `lessons/` | 踩坑/发现经验教训时 | 新增教训文档 |

---

## 文档联动规则

修改一个文档时，必须联动检查相关文档。详见 `docs/knowledge-base/README.md` 中的文档联动规则表。

---

## 变更记录填写规范

每个被更新的文档必须填写变更记录，格式：

```text
| 日期 | 变更内容 | 变更人 | 关联变更 |
|------|----------|--------|----------|
| 2024-01-15 | 新增用户注册不变量 | 张三 | #TASK-42 同步更新 boundaries.md |
```

**关联变更**字段填写规则：

- 同一批次的变更填写相同标记（如任务编号 `#TASK-42`）
- 联动更新的文档在此字段互相引用
- 初始化时填写 `—`

---

## 必须输出

```text
【知识库更新结果】

需要更新：

| 文档 | 更新内容 | 联动检查 |
|------|----------|----------|
| xxx.md | 具体更新内容 | 已检查 yyy.md（无需更新/需同步更新：内容） |

变更记录已更新：是/否
```

或者：

```text
【知识库更新结果】

本次无需更新知识库。
```

---

# 知识库与代码不一致时

发现历史不一致（非当前开发引入）时：以代码实现为准，并立即更新相关知识库文档。

与"文档优先原则"的区别：
- 文档优先原则：开发过程中，先改文档再改代码（主动）
- 以代码为准：发现历史不一致时，以代码为真相修正文档（被动）

禁止长期存在代码与知识库不一致的情况。

---

# 知识库健康度检查

定期或在重大变更后，执行健康度检查。详见 `docs/knowledge-base/README.md` 中的健康度检查清单。

---

# 最终目标

```text
知识库驱动开发

设计先于编码

文档先于实现

测试保证质量

代码与知识同步

设计与实现一致
```
